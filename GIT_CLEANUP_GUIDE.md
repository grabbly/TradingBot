# 🔒 Инструкция по очистке Git истории от чувствительных данных

## ⚠️ ВАЖНО: Выполните ДО публикации репозитория!

Если вы уже закоммитили файлы с секретами в Git, их нужно удалить из истории.

---

## 📋 Что было найдено и исправлено

### ✅ Исправленные файлы (теперь безопасны):
- `check_portfolio.py` - убраны hardcoded API ключи
- `check_orders.py` - убраны hardcoded API ключи  
- `scripts/check_positions_alpaca.py` - убраны hardcoded API ключи
- `scripts/load_historical_data.py` - убран hardcoded пароль БД
- `scripts/backtest_golden_cross.py` - убран hardcoded пароль БД
- `scripts/backtest_weekly_ema.py` - убран hardcoded пароль БД
- `scripts/backtest_ema_strategy.py` - убран hardcoded пароль БД
- `scripts/plot_ema.py` - убран hardcoded пароль БД и IP
- `web/app.py` - убран hardcoded пароль БД
- `.env` - очищен от реальных данных (теперь template)
- `.env.example` - очищен от реальных паролей
- `.gitignore` - обновлён для защиты чувствительных файлов

### 🚨 Чувствительные данные которые были в репозитории:
- Alpaca API keys: `PKX2Y2J57QRKG5HVGZ4IRKG7TG` и секрет
- Finnhub API key: `d5vp0lhr01qihi8n877gd5vp0lhr01qihi8n8780`
- PostgreSQL пароль: `n8n_secure_pass_2024`
- Локальный IP: `192.168.1.3`
- Файлы в `.docs/` с потенциальными секретами

---

## 🛠️ Шаги по очистке Git истории

### Вариант 1: BFG Repo-Cleaner (рекомендуется - быстрый и простой)

```bash
# 1. Установите BFG (если ещё не установлен)
brew install bfg   # macOS
# или скачайте с https://rtyley.github.io/bfg-repo-cleaner/

# 2. Создайте список секретов для удаления
cat > secrets.txt << 'EOF'
PKX2Y2J57QRKG5HVGZ4IRKG7TG
GzgSzBh7YKE4jagtgqRo6SxhGLZK9BXQoj4d6Fzqj2wx
d5vp0lhr01qihi8n877gd5vp0lhr01qihi8n8780
n8n_secure_pass_2024
n8n_secure_pass_202
192.168.1.3
EOF

# 3. Сделайте резервную копию репозитория
cd /Users/gabby/git
cp -r TradingBot TradingBot.backup

# 4. Запустите очистку
cd TradingBot
bfg --replace-text secrets.txt

# 5. Очистите историю и проверьте
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# 6. Удалите файл с секретами
rm ../secrets.txt

# 7. Проверьте что секретов нет
git log --all --full-history -- "*" | grep -i "PKX2Y2J57QRKG5HVGZ4IRKG7TG" || echo "✅ Секреты удалены"
```

### Вариант 2: git-filter-repo (более гибкий)

```bash
# 1. Установите git-filter-repo
brew install git-filter-repo   # macOS
# или: pip3 install git-filter-repo

# 2. Создайте резервную копию
cd /Users/gabby/git
cp -r TradingBot TradingBot.backup

# 3. Удалите файлы из истории
cd TradingBot
git-filter-repo --invert-paths \
  --path .env \
  --path .docs/ \
  --force

# 4. Замените секреты в оставшихся файлах
git-filter-repo --replace-text <(cat << 'EOF'
PKX2Y2J57QRKG5HVGZ4IRKG7TG==>ALPACA_API_KEY_REMOVED
GzgSzBh7YKE4jagtgqRo6SxhGLZK9BXQoj4d6Fzqj2wx==>ALPACA_SECRET_REMOVED
d5vp0lhr01qihi8n877gd5vp0lhr01qihi8n8780==>FINNHUB_KEY_REMOVED
n8n_secure_pass_2024==>PASSWORD_REMOVED
192.168.1.3==>localhost
EOF
) --force
```

### Вариант 3: Начать заново (самый безопасный для публичных репозиториев)

```bash
# 1. Сохраните текущий код
cd /Users/gabby/git
cp -r TradingBot TradingBot.backup

# 2. Удалите .git директорию
cd TradingBot
rm -rf .git

# 3. Создайте новый репозиторий
git init
git add .
git commit -m "Initial commit - cleaned version"

# 4. Настройте новый remote (если нужно)
# git remote add origin YOUR_NEW_REPO_URL
# git push -u origin main
```

---

## 🔐 После очистки: Обязательные действия

### 1. Отзовите все API ключи

#### Alpaca (КРИТИЧНО!)
1. Перейдите: https://app.alpaca.markets/paper/dashboard/overview
2. Удалите старые ключи: `PKX2Y2J57QRKG5HVGZ4IRKG7TG`
3. Создайте новые ключи
4. Обновите `.env` локально (НЕ коммитьте!)

#### Finnhub
1. Перейдите: https://finnhub.io/dashboard
2. Отзовите ключ: `d5vp0lhr01qihi8n877gd5vp0lhr01qihi8n8780`
3. Создайте новый ключ
4. Обновите `.env` локально

### 2. Смените пароль PostgreSQL

```bash
# Подключитесь к PostgreSQL
psql -h localhost -U postgres

# Смените пароль
ALTER USER n8n_user WITH PASSWORD 'ваш_новый_безопасный_пароль';
\q

# Обновите .env локально
```

### 3. Настройте .env для работы

```bash
# Скопируйте template
cp .env .env.local

# Заполните .env реальными значениями
nano .env

# Убедитесь что .env в .gitignore
cat .gitignore | grep "^\.env$"
```

---

## ✅ Проверка перед публикацией

```bash
# 1. Проверьте что нет чувствительных данных
git log --all --full-history --source --grep="PKX2Y2J57QRKG5HVGZ4IRKG7TG"
git log --all --full-history --source --grep="n8n_secure_pass"

# 2. Проверьте все файлы в репозитории
git grep -i "PKX2Y2J57QRKG5HVGZ4IRKG7TG" $(git rev-list --all)
git grep -i "n8n_secure_pass" $(git rev-list --all)

# 3. Проверьте что .env не в git
git ls-files | grep "^\.env$" && echo "⚠️ .env в репозитории!" || echo "✅ .env не отслеживается"

# 4. Проверьте что .docs не в git
git ls-files | grep "\.docs/" && echo "⚠️ .docs в репозитории!" || echo "✅ .docs не отслеживается"

# 5. Проверьте .gitignore
cat .gitignore
```

---

## 📤 Публикация в GitHub

После очистки и проверки:

```bash
# 1. Создайте новый репозиторий на GitHub (через веб интерфейс)
#    НЕ инициализируйте с README, если уже есть локальный

# 2. Добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/TradingBot.git

# 3. Запушьте (используйте --force только для нового репозитория!)
git push -u origin main

# ИЛИ для существующего репозитория ПОСЛЕ очистки:
# git push --force-with-lease origin main
```

---

## ⚠️ Важные напоминания

1. **НИКОГДА не используйте `--force` push если кто-то уже клонировал репозиторий!**
2. **После force push все секреты ВСЕ ЕЩЁ могут быть доступны** в старых клонах
3. **Отозвать API ключи КРИТИЧЕСКИ ВАЖНО** даже после очистки истории
4. **Используйте `.env` только локально**, никогда не коммитьте
5. **Регулярно проверяйте** что не добавляете секреты случайно

---

## 📚 Дополнительные ресурсы

- [BFG Repo-Cleaner](https://rtyley.github.io/bfg-repo-cleaner/)
- [git-filter-repo](https://github.com/newren/git-filter-repo)
- [GitHub: Removing sensitive data](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)
- [Git Secrets Scanner](https://github.com/trufflesecurity/trufflehog)

---

## 🆘 Если секреты уже в публичном репозитории

1. **Немедленно отзовите ВСЕ ключи**
2. Смените все пароли
3. Проверьте логи Alpaca/Finnhub на подозрительную активность
4. Удалите репозиторий с GitHub (если возможно)
5. Создайте новый репозиторий с очищенной историей
6. Уведомите команду (если работаете не один)

---

**Удачи! После выполнения всех шагов ваш репозиторий будет безопасен для публикации.**
