# EMA Quick Reference — Где участвуют EMA данные?

## 🎯 Самый Быстрый Ответ

**EMA Logger** (каждую минуту) → **ema_snapshots** таблица → **Sentiment Executor** (18:00 EST, Пн-Пт) использует **3 критичных EMA** + **RSI14** для генерации **BUY сигналов** в Alpaca.

---

## 📊 Жизненный Цикл EMA Данных

```
┌─ MINUTE 1, 2, 3... (24/7) ─────────────────────────────────────┐
│                                                                 │
│  EMA Logger Workflow (n8n)                                      │
│  ├─ GET latest OHLCV from Alpaca API                           │
│  ├─ CALCULATE:                                                 │
│  │  ├─ EMA5, EMA8, EMA9, EMA13, EMA20, EMA21 (краткие)         │
│  │  ├─ EMA34, EMA50, EMA100, EMA200 (средние/длинные)         │
│  │  ├─ RSI14 (импульс)                                        │
│  │  └─ Volume MA20 (подтверждение объема)                     │
│  └─ WRITE to PostgreSQL ema_snapshots table                    │
│     └─ 1 запись = 1 минута × 1 символ (7 символов = 7 записей)
│
└─────────────────────────────────────────────────────────────────┘

┌─ 18:00 EST (ТОЛЬКО ПОНЕДЕЛЬНИК-ПЯТНИЦА) ────────────────────┐
│                                                              │
│  Sentiment Executor Workflow (n8n)                           │
│  ├─ FETCH latest ema_snapshot (последняя минута перед 18:00)│
│  │  └─ SELECT * FROM ema_snapshots WHERE timestamp=MAX()     │
│  │                                                           │
│  ├─ APPLY 4 FILTERS (ВСЕ MUST BE TRUE):                      │
│  │  │                                                        │
│  │  ├─ 🔴 FILTER 1: close_price > EMA200                    │
│  │  │   (цена выше 200-дневного тренда = восходящий тренд)  │
│  │  │                                                        │
│  │  ├─ 🔴 FILTER 2: EMA9 > EMA21                            │
│  │  │   (краткий тренд > средний = "Golden Cross" = BUY)    │
│  │  │                                                        │
│  │  ├─ 🔴 FILTER 3: RSI14 < 70                              │
│  │  │   (не перекуплено, еще есть место для роста)          │
│  │  │                                                        │
│  │  └─ 🔴 FILTER 4: volume > volume_ma20                    │
│  │      (объем > среднего = подтверждение движения)         │
│  │                                                           │
│  ├─ IF ALL PASS:                                             │
│  │  ├─ Generate BUY signal                                   │
│  │  ├─ Calculate position size (1% risk, 2% SL, 4% TP)       │
│  │  ├─ Send bracket order to Alpaca                         │
│  │  └─ Send Telegram alert                                  │
│  │                                                           │
│  └─ IF ANY FAIL: Skip this symbol, try next                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔴 3 КРИТИЧНЫХ EMA ПЕРИОДА

### 1️⃣ EMA200 — "Основной Тренд" (ФИЛЬТР 1)

- **Период:** 200 дней
- **Использование:** `IF close_price > EMA200 THEN PASS`
- **Смысл:** Если цена выше EMA200, мы в восходящем тренде
- **Пример:** 
  - NVDA close: 138.45, EMA200: 136.20 → 138.45 > 136.20 ✅ PASS
  - AAPL close: 145.00, EMA200: 147.50 → 145.00 < 147.50 ❌ FAIL

---

### 2️⃣ EMA9 > EMA21 — "Golden Cross" (ФИЛЬТР 2)

- **EMA9:** Краткосрочный тренд (9 дней)
- **EMA21:** Среднесрочный тренд (21 день)
- **Использование:** `IF EMA9 > EMA21 THEN PASS`
- **Смысл:** Когда EMA9 пересекает EMA21 снизу вверх — это "Golden Cross" = сильный BUY сигнал
- **Пример:**
  - NVDA EMA9: 140.10, EMA21: 139.50 → 140.10 > 139.50 ✅ PASS (Golden Cross!)
  - TSLA EMA9: 240.00, EMA21: 242.00 → 240.00 < 242.00 ❌ FAIL (Death Cross)

---

### 3️⃣ EMA200 + EMA9/21 Комбинация = Идеальный Setup

```
STRONG BUY SIGNAL когда:
  • Цена выше EMA200 ← восходящий долгий тренд ✅
  AND
  • EMA9 > EMA21 ← краткий тренд выше среднего ✅
  AND
  • RSI14 < 70 ← не перекуплено ✅
  AND
  • Volume > MA20 ← подтверждение ✅

ЕСЛИ ВСЕ 4 = TRUE → генерируем BUY сигнал
```

---

## 📈 Остальные 6 EMAs (Резерв, Информация)

| EMA | Период | Назначение | Используется |
|-----|--------|-----------|--------------|
| EMA5 | 5 дней | Ультра-краткий тренд | Только визуализация |
| EMA8 | 8 дней | Краткий | Резерв для будущих стратегий |
| EMA13 | 13 дней | Альтернативный краткий | Только информация |
| EMA20 | 20 дней | Стандартный краткий | Резерв |
| EMA34 | 34 дня | Средний Фибо | Визуализация |
| EMA50 | 50 дней | Классический средний | Визуализация |
| EMA100 | 100 дней | Длинный | Визуализация |

**Итого:** 7 EMAs служат для **информации и графиков**, но **не участвуют** в решении о BUY/SELL.

---

## 🔍 Реальный Пример (Пятница, 18:00 EST)

```
SNAPSHOT: 2026-02-07 17:59 (последняя минута перед 18:00)

SYMBOL: NVDA
├─ close_price: 138.45
├─ ema200: 136.20
├─ ema9: 140.10
├─ ema21: 139.50
├─ rsi14: 62.3
├─ volume: 45,230,000
└─ volume_ma20: 38,000,000

FILTERS:
1️⃣ 138.45 > 136.20? YES ✅
2️⃣ 140.10 > 139.50? YES ✅
3️⃣ 62.3 < 70? YES ✅
4️⃣ 45,230,000 > 38,000,000? YES ✅

RESULT: 🟢 BUY SIGNAL GENERATED
         Entry: $138.45 | QTY: 180 | SL: $135.69 | TP: $144.12
```

---

## 📊 Database Structure

### ema_snapshots Table (PostgreSQL)

```sql
CREATE TABLE ema_snapshots (
  id SERIAL PRIMARY KEY,
  timestamp TIMESTAMPTZ NOT NULL,        -- Когда было создано
  symbol VARCHAR(20) NOT NULL,           -- AAPL, NVDA, TSLA, etc.
  close_price DECIMAL(12, 4) NOT NULL,   -- Рыночная цена
  
  -- 10 EMA периодов (используются для техфильтров и графиков)
  ema5 DECIMAL(12, 4),    ema8 DECIMAL(12, 4),
  ema9 DECIMAL(12, 4),    ema13 DECIMAL(12, 4),
  ema20 DECIMAL(12, 4),   ema21 DECIMAL(12, 4),
  ema34 DECIMAL(12, 4),   ema50 DECIMAL(12, 4),
  ema100 DECIMAL(12, 4),  ema200 DECIMAL(12, 4),
  
  -- Дополнительные индикаторы
  rsi14 DECIMAL(12, 4),                  -- Relative Strength Index
  volume BIGINT,                         -- Объем текущей свечи
  volume_ma20 DECIMAL(12, 4),            -- MA20 объема
  
  -- Metadata
  action VARCHAR(20),                    -- BUY, SELL, HOLD
  crossover VARCHAR(10),                 -- GOLD_UP, DEATH_DOWN, NONE
  message TEXT                           -- Описание
);

-- Индексы для быстрого поиска (используется Sentiment Executor)
CREATE INDEX idx_ema_symbol ON ema_snapshots(symbol);
CREATE INDEX idx_ema_timestamp ON ema_snapshots(timestamp DESC);
```

---

## 🔄 Query Используется в Sentiment Executor

```sql
-- Каждый раз в 18:00 EST, для каждого символа:
SELECT 
  symbol, timestamp, close_price,
  ema9, ema21, ema200, rsi14,
  volume, volume_ma20,
  action, crossover
FROM ema_snapshots
WHERE symbol = 'NVDA'
  AND timestamp = (SELECT MAX(timestamp) FROM ema_snapshots WHERE symbol = 'NVDA')
LIMIT 1;

-- Результат: Последний snapshot перед 18:00 (обычно 17:59 или раньше)
```

---

## ⏱️ Расписание

| Время (EST) | Workflow | Действие |
|---|---|---|
| 00:00 - 23:59 | **EMA Logger** | Собирает данные КАЖДУЮ МИНУТУ (1,440 записей/символ в день) |
| 18:00 (ПН-ПТ) | **Sentiment Executor** | Читает ПОСЛЕДНИЙ snapshot из ema_snapshots, применяет 4 фильтра, генерирует сигналы |
| 09:30-16:00 (Пн-Пт) | **Alpaca API** | Исполняет заказанные bracket orders (если сигнал был) |

---

## 💡 Ключные Выводы

1. **EMA Logger работает 24/7** каждую минуту, собирая свежие OHLCV данные
2. **Только 3 EMA периода имеют значение** для торговых сигналов: EMA200, EMA9, EMA21
3. **Sentiment Executor читает последний snapshot** за секунду до 18:00 EST
4. **Все 4 фильтра должны быть TRUE** для генерации BUY сигнала
5. **Остальные 6 EMAs** — только для графиков и будущих стратегий
6. **RSI14 и Volume MA20** — дополнительные подтверждающие индикаторы
7. **Данные записываются в PostgreSQL** каждую минуту (готовы к лучшей стратегии позже)

---

**Документ:** EMA Quick Reference  
**Обновлено:** Февраль 2026  
**Статус:** Production Ready ✅
