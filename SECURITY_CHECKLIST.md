# 🚀 Быстрый старт: Подготовка к публикации

## ✅ Что уже сделано

Проект **очищен от чувствительных данных** и готов к дальнейшей подготовке для публикации:

### Исправлено:
- ✅ Обновлён `.gitignore` - теперь защищает `.env`, `.docs/`, `data/`, `reports/`
- ✅ Все Python скрипты используют переменные окружения вместо hardcoded credentials
- ✅ `.env` файл очищен и превращён в template
- ✅ `.env.example` содержит только placeholder значения
- ✅ Создана полная инструкция по очистке Git истории: `GIT_CLEANUP_GUIDE.md`

---

## ⚠️ ОБЯЗАТЕЛЬНО ПЕРЕД ПУБЛИКАЦИЕЙ

### 1️⃣ Отзовите ВСЕ API ключи

#### 🔑 Alpaca (КРИТИЧНО!)
```
Старые ключи (УТЕКЛИ):
- API Key: PKX2Y2J57QRKG5HVGZ4IRKG7TG
- Secret: GzgSzBh7YKE4jagtgqRo6SxhGLZK9BXQoj4d6Fzqj2wx

Действия:
1. https://app.alpaca.markets/paper/dashboard/overview
2. Удалить старые ключи
3. Создать новые ключи
4. Обновить локальный .env файл
```

#### 🔑 Finnhub
```
Старый ключ (УТЁК): d5vp0lhr01qihi8n877gd5vp0lhr01qihi8n8780

Действия:
1. https://finnhub.io/dashboard
2. Отозвать старый ключ
3. Создать новый ключ
4. Обновить локальный .env файл
```

### 2️⃣ Смените пароль PostgreSQL

```bash
psql -h localhost -U postgres
ALTER USER n8n_user WITH PASSWORD 'новый_безопасный_пароль';
\q
```

### 3️⃣ Очистите Git историю

**Выберите один из методов из `GIT_CLEANUP_GUIDE.md`:**

#### Быстрый способ (рекомендуется):
```bash
# Создайте backup
cp -r /Users/gabby/git/TradingBot /Users/gabby/git/TradingBot.backup

# Используйте BFG Repo-Cleaner
cd /Users/gabby/git/TradingBot
brew install bfg

# Создайте список секретов
cat > /tmp/secrets.txt << 'EOF'
PKX2Y2J57QRKG5HVGZ4IRKG7TG
GzgSzBh7YKE4jagtgqRo6SxhGLZK9BXQoj4d6Fzqj2wx
d5vp0lhr01qihi8n877gd5vp0lhr01qihi8n8780
n8n_secure_pass_2024
192.168.1.3
EOF

# Очистите
bfg --replace-text /tmp/secrets.txt
git reflog expire --expire=now --all
git gc --prune=now --aggressive
rm /tmp/secrets.txt
```

### 4️⃣ Проверьте что секретов нет

```bash
cd /Users/gabby/git/TradingBot

# Проверьте в текущих файлах
git grep -i "PKX2Y2J57QRKG5HVGZ4IRKG7TG" || echo "✅ OK"
git grep -i "n8n_secure_pass" || echo "✅ OK"

# Проверьте что .env не в репозитории
git ls-files | grep "^\.env$" && echo "⚠️ ПРОБЛЕМА!" || echo "✅ OK"

# Проверьте что .docs не в репозитории
git ls-files | grep "\.docs/" && echo "⚠️ ПРОБЛЕМА!" || echo "✅ OK"
```

---

## 📝 Настройка локального окружения

После очистки создайте локальный `.env` файл:

```bash
cd /Users/gabby/git/TradingBot

# Создайте .env из template
cp .env .env.local

# Отредактируйте и добавьте реальные данные
nano .env

# .env должен содержать:
# ALPACA_API_KEY=ваш_новый_ключ
# ALPACA_SECRET_KEY=ваш_новый_секрет
# POSTGRES_PASSWORD=ваш_новый_пароль
# FINNHUB_API_KEY=ваш_новый_ключ
# и т.д.
```

⚠️ **НИКОГДА не коммитьте .env файл!**

---

## 🌐 Публикация на GitHub

После выполнения всех шагов выше:

```bash
cd /Users/gabby/git/TradingBot

# Добавьте изменения
git add .
git commit -m "Security: Remove hardcoded credentials, update to use environment variables"

# Создайте новый репозиторий на GitHub (через веб)
# Затем добавьте remote:
git remote add origin https://github.com/YOUR_USERNAME/TradingBot.git

# Запушьте (если это НОВЫЙ репозиторий)
git push -u origin main

# ИЛИ force push если очистили историю (ОСТОРОЖНО!)
# git push --force-with-lease origin main
```

---

## 📚 Важные файлы

- **`GIT_CLEANUP_GUIDE.md`** - Подробная инструкция по очистке Git истории
- **`.gitignore`** - Обновлён для защиты чувствительных данных
- **`.env.example`** - Template для настройки окружения
- **`.env`** - Локальный файл (НЕ КОММИТИТЬ!)

---

## 🔒 Контрольный список перед публикацией

- [ ] Отозваны Alpaca API ключи
- [ ] Отозван Finnhub API ключ
- [ ] Изменён пароль PostgreSQL
- [ ] Очищена Git история (BFG или git-filter-repo)
- [ ] Проверено что секретов нет: `git grep -i "PKX2Y2J"`
- [ ] `.env` файл в `.gitignore` и не отслеживается
- [ ] `.docs/` директория в `.gitignore` и не отслеживается
- [ ] Создан локальный `.env` с новыми credentials
- [ ] Протестированы скрипты с новыми credentials

---

## ❓ Вопросы?

Смотрите **`GIT_CLEANUP_GUIDE.md`** для подробных инструкций.

**Удачи! 🚀**
