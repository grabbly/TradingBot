Автоматизация свинг-трейдинг стратегии Dual EMA Crossover с регулируемым подтверждением входа через n8n и Alpaca Markets API

## Alpaca Markets API

Base URLs:
- Paper Trading: https://paper-api.alpaca.markets
- Live Trading: https://api.alpaca.markets
- Market Data: https://data.alpaca.markets

Аутентификация:
- Header: APCA-API-KEY-ID: {API_KEY}
- Header: APCA-API-SECRET-KEY: {SECRET_KEY}

Ключевые эндпоинты:
- GET /v2/account — информация об аккаунте
- GET /v2/positions — текущие позиции
- GET /v2/positions/{symbol} — позиция по символу
- POST /v2/orders — создание ордера
- DELETE /v2/positions/{symbol} — закрытие позиции
- GET /v2/bars/{symbol} — OHLC данные (Market Data API)

---

Основная идея стратегии
Торгуем только long-позиции (flat → long → flat).
Сигналы генерируются исключительно по пересечению двух экспоненциальных скользящих средних:

Короткая EMA — период 5
Длинная EMA — период 20

Правила входа и выхода

Вход в позицию (Buy)
Зафиксировано bullish crossover: 5 EMA пересекла 20 EMA снизу вверх (на закрытии свечи).
Запоминаем цену закрытия свечи пересечения (цена_кросса).
Далее ждём подтверждения роста: текущая цена должна подняться на X% выше цены_кросса.
X — регулируемый параметр (по умолчанию 0.75%, возможные значения 0.3–2.0%).

Как только цена ≥ цена_кросса × (1 + X/100) → открываем long-позицию (market order).
Если рост не произошёл и появился bearish crossover — сигнал отменяется.

Выход из позиции (Sell / Close)
Зафиксировано bearish crossover: 5 EMA пересекла 20 EMA сверху вниз (на закрытии свечи).
Полностью закрываем открытую long-позицию (market order).
Никаких дополнительных условий на выходе (чтобы ловить весь тренд).


Таймфрейм и инструменты

Рекомендуемый таймфрейм: 1-часовой (1h) или 4-часовой (4h) — оптимально для свинг-трейдинга.
Инструмент: на старте выбрать один трендовый актив (например, AAPL.US, NVDA.US, SPY.US, BTC.USD или ES.FUT).
Расчёт EMA и проверка условий — строго по закрытии каждой новой свечи.

Минимальная задержка

После закрытия свечи бот должен получить данные и принять решение в течение 10–30 секунд.
Использовать polling OHLC через Alpaca Market Data API:
GET https://data.alpaca.markets/v2/stocks/{symbol}/bars?timeframe=1Hour&limit=50

Альтернатива (если потребуется быстрее): WebSocket streaming через wss://stream.data.alpaca.markets

Риск-менеджмент (обязательно на старте)

Стоп-лосс: фиксированный 2–3% ниже цены входа (или ниже 20 EMA на момент входа).
Размер позиции: фиксированный лот (настраивается) или 1–2% риска от баланса.
Максимум одна открытая позиция по инструменту.

Логирование и уведомления

Все события (crossover, подтверждение роста, открытие/закрытие позиции) записываются в Google Sheets: дата, символ, направление, цена входа/выхода, значения EMA, % подтверждения.
Уведомления в Telegram: текст + ключевые параметры сделки.

Структура workflow в n8n

Trigger — Cron или Interval (каждую минуту для 1h таймфрейма).
HTTP Request — получить последние OHLC свечи от Alpaca (GET /v2/stocks/{symbol}/bars).
Function Node — расчёт текущих 5 EMA и 20 EMA + сохранение предыдущих значений.
Function Node — детекция crossover и логика подтверждения роста в % (с регулируемым параметром X).
HTTP Request — запрос текущих позиций (GET /v2/positions/{symbol}) для проверки состояния.
If Nodes — условия на buy/sell.
HTTP Request — размещение market-ордера (POST /v2/orders) или закрытие позиции (DELETE /v2/positions/{symbol}).
Google Sheets + Telegram Nodes — логирование и алерты.
Error Handling — обработка ошибок API и уведомления.

Тестирование

Бэктест — отдельный workflow на исторических данных Alpaca (минимум 2–3 года).
Paper Trading аккаунт Alpaca — запуск бота без риска (реальные данные, виртуальные деньги).
Оптимизация параметра X% — тестировать значения 0.5%, 0.75%, 1.0%, 1.5% и выбрать лучший по Sharpe/прибыльности/просадке.
Только после положительных результатов — переход на реальный аккаунт.

Параметры для настройки в боте

Символ (symbolId)
Таймфрейм (duration: 1h / 4h)
Периоды EMA (5 и 20 — фиксировано на старте)
Процент подтверждения роста (X%) — регулируемый
Размер позиции
Процент стоп-лосса

Эта версия стратегии сочетает агрессивность короткой 5 EMA с фильтрацией ложных сигналов через регулируемое подтверждение роста — отличный баланс для свинг-трейдинга.
Готов к реализации!
Следующий шаг: выбери конкретный инструмент и таймфрейм (например, NVDA.US на 1h), и я сразу дам готовый JSON-шаблон workflow для импорта в n8n + пример кода для расчёта EMA и логики подтверждения. 🚀